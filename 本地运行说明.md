# 后端本地运行说明

## MySQL 连接失败（Access denied）的解决方式

错误 `Access denied for user 'root'@'localhost' (using password: YES)` 表示 MySQL 用户名或密码不正确。

### 方式一：设置环境变量（推荐）

在 IDEA 中：
1. Run → Edit Configurations
2. 选择 `FinalYearProjectApplication`
3. 在 **Environment variables** 中添加：
   - `MYSQL_PASSWORD` = 你的 MySQL root 密码

如果 root 无密码，可添加 `MYSQL_PASSWORD=`（空值）。

### 方式二：使用 application-local.yml

1. 复制 `src/main/resources/application-local.yml.example` 为 `application-local.yml`
2. 编辑 `application-local.yml`，将 `你的MySQL密码` 改为实际密码
3. 在 `application.yml` 中，将 `spring.profiles.active: dev` 改为 `spring.profiles.active: dev,local`
   - 或在 IDEA 的 Program arguments 中添加：`--spring.profiles.active=dev,local`

### 方式三：直接修改 application-dev.yml（不推荐，易被提交）

在 `application-dev.yml` 中修改 `password` 为你的 MySQL 密码（当前已改为从环境变量读取）。

---

## 前置条件

- **MySQL** 已安装并启动，端口 3306
- **Redis** 已安装并启动，端口 6379
- 数据库 `note_recommend_db` 会在首次连接时自动创建（需 `createDatabaseIfNotExist=true`）
